{
    "version": "pain-unbc-v1",
    "format": "onnx",
    "description": "Pain detection model trained on UNBC-McMaster Shoulder Pain Expression Archive. Outputs PSPI pain score normalized to 0-10 scale.",
    "trained_on": "UNBC-McMaster (subjects 66, 80, 97, 108, 121 excluded from training)",
    "source": "TaatiTeam/pain_detection_demo (converted from PyTorch with zero-reference wrapper)",
    "input": "1x1x160x160 grayscale",
    "output": "Single regression value (0-10 pain score)",
    "notes": "Original model is comparative (target + reference frame). Wrapped with zero-reference for single-image inference. Subject-specific calibration not available in this mode."
}
